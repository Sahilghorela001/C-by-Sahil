#include <stdio.h>
#include <conio.h>
int main()
{ 
    float a, b, total;
    printf("enter two numbes -");
    scanf("%f%f",&a, &b);
    total= a+b;
    printf(" sum of %f and %f is %f",a,b,total);
    getch();
    return 0;
}
    